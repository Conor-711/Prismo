// Set through CI build settings or local Supabase.xcconfig.local (gitignored).
// Public publishable key only. Never use service_role or sb_secret_ keys.
BSMART_SUPABASE_PUBLISHABLE_KEY = sb_publishable_REPLACE_WITH_PROJECT_PUBLIC_KEY
