# Runtime SQLite databases are intentionally not tracked. Railway restores the
# compressed deployment snapshot generated by `make snapshot-db`.
data/reports/sv_segment_backtest/sv_segment_event_results.csv filter=lfs diff=lfs merge=lfs -text
